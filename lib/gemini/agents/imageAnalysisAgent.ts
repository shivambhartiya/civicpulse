import { getGeminiModel, parseJson } from '../client';
import type { ImageAnalysisResult } from '@/lib/types/agents';
export const fallbackImageAnalysis: ImageAnalysisResult = { isIssue: true, confidence: 0.88, suggestedTitle: 'Road surface hazard detected', suggestedDescription: 'Photo and context indicate a civic infrastructure issue requiring municipal triage.', suggestedCategory: 'POTHOLE', suggestedSubcategory: 'road_surface_damage', suggestedDepartment: 'Public Works', suggestedSeverity: 7, detectedObjects: ['road', 'surface break', 'traffic risk'], safetyAssessment: 'medium', reasoning: 'Fallback analysis used because Gemini credentials are not configured.' };
export async function analyzeIssueImage(imageBase64: string, mimeType: string, userDescription = ''): Promise<ImageAnalysisResult> { const model = getGeminiModel(); if (!model) return { ...fallbackImageAnalysis, suggestedDescription: userDescription || fallbackImageAnalysis.suggestedDescription }; const prompt = 'Analyze this civic issue image. Return JSON with isIssue, confidence, suggestedTitle, suggestedDescription, suggestedCategory (POTHOLE,WATER,LIGHTING,WASTE,ROAD,BUILDING,OTHER), suggestedSubcategory, suggestedDepartment, suggestedSeverity 1-10, detectedObjects, safetyAssessment low|medium|high, reasoning. User description: ' + userDescription; const result = await model.generateContent([prompt, { inlineData: { data: imageBase64, mimeType } }]); return parseJson<ImageAnalysisResult>(result.response.text(), fallbackImageAnalysis); }
