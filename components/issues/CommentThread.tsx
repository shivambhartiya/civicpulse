import { Avatar, AvatarFallback } from '@/components/ui/avatar'; import { Textarea } from '@/components/ui/textarea'; import { Button } from '@/components/ui/button'; import type { IssueComment } from '@/lib/types/issue'; import { timeAgo } from '@/lib/utils/formatting';
export function CommentThread({ comments }: { comments: IssueComment[] }) { return <div className="space-y-4">{comments.map((comment) => <div key={comment.id} className="flex gap-3"><Avatar><AvatarFallback>{comment.authorName.slice(0,2).toUpperCase()}</AvatarFallback></Avatar><div><p className="text-sm font-medium">{comment.authorName} <span className="font-normal text-muted-foreground">{timeAgo(comment.createdAt)}</span></p><p className="text-sm text-muted-foreground">{comment.body}</p></div></div>)}<div className="space-y-2"><Textarea placeholder="Add local evidence, repair updates, or context" /><Button>Post comment</Button></div></div>; }
