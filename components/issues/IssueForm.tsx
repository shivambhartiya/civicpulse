'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { motion, AnimatePresence } from 'framer-motion'; import { Bot, Check, LocateFixed, Mic, Sparkles } from 'lucide-react'; import { Button } from '@/components/ui/button'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import { Input } from '@/components/ui/input'; import { Textarea } from '@/components/ui/textarea'; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'; import { PhotoUploader } from '@/components/shared/PhotoUploader'; import { LocationPicker } from '@/components/map/LocationPicker'; import { ISSUE_CATEGORIES } from '@/lib/constants/categories'; import { useReportStore } from '@/store/reportStore'; import type { IssueCategory } from '@/lib/types/issue';
const steps = ['Capture', 'AI verify', 'Locate'];
export function IssueForm() { const [step, setStep] = useState(0); const [isAnalyzing, setIsAnalyzing] = useState(false); const { formData, setFormData, reset } = useReportStore(); const router = useRouter(); async function handleFile(file: File) { setFormData({ imageFile: file, imagePreview: URL.createObjectURL(file) }); setStep(1); setIsAnalyzing(true); const fd = new FormData(); fd.append('image', file); fd.append('description', formData.description ?? ''); const res = await fetch('/api/ai/analyze-image', { method: 'POST', body: fd }); const analysis = await res.json(); setFormData({ title: analysis.suggestedTitle, description: analysis.suggestedDescription, category: analysis.suggestedCategory, severity: analysis.suggestedSeverity, aiAnalysis: analysis }); setIsAnalyzing(false); }
async function submit() { const res = await fetch('/api/issues', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title: formData.title, description: formData.description, category: formData.category, severity: formData.severity, subcategory: formData.aiAnalysis?.suggestedSubcategory || 'general', images: formData.imagePreview ? [formData.imagePreview] : [], location: formData.location, aiAnalysis: formData.aiAnalysis }) }); const data = await res.json(); reset(); router.push('/issues/' + data.issue.id); }
return <Card><CardHeader><CardTitle>Report an issue</CardTitle><div className="grid grid-cols-3 gap-2 pt-3">{steps.map((label, index) => <div key={label} className={index <= step ? 'h-2 rounded bg-primary' : 'h-2 rounded bg-muted'} />)}</div></CardHeader><CardContent><AnimatePresence mode="wait"><motion.div key={step} initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -8 }} className="space-y-5">{step === 0 ? <><PhotoUploader onFile={handleFile} /><Button type="button" variant="outline" onClick={() => setFormData({ description: 'Voice note: broken road surface blocking safe commute near my lane.' })}><Mic className="h-4 w-4" /> Voice report mode</Button><Textarea value={formData.description ?? ''} onChange={(e) => setFormData({ description: e.target.value })} placeholder="Optional context before AI analysis" /></> : null}{step === 1 ? <div className="space-y-4">{isAnalyzing ? <div className="rounded-lg border p-6 text-center"><Sparkles className="mx-auto h-8 w-8 animate-pulse text-primary" /><p className="mt-2 font-medium">Gemini agents are analyzing image, severity, routing, and duplicate risk...</p></div> : <><div className="rounded-lg border bg-primary/10 p-4 text-sm"><Bot className="mb-2 h-5 w-5 text-primary" />AI categorized this as {formData.category} with severity {formData.severity}/10.</div><Input value={formData.title ?? ''} onChange={(e) => setFormData({ title: e.target.value })} placeholder="Issue title" /><Textarea value={formData.description ?? ''} onChange={(e) => setFormData({ description: e.target.value })} /><Select value={formData.category} onValueChange={(value) => setFormData({ category: value as IssueCategory })}><SelectTrigger><SelectValue placeholder="Category" /></SelectTrigger><SelectContent>{ISSUE_CATEGORIES.map((cat) => <SelectItem key={cat} value={cat}>{cat}</SelectItem>)}</SelectContent></Select><Button onClick={() => setStep(2)}>Continue to location</Button></>}</div> : null}{step === 2 && formData.location ? <div className="space-y-4"><LocationPicker value={formData.location} onChange={(location) => setFormData({ location })} /><Button onClick={submit} className="w-full"><Check className="h-4 w-4" />Submit and earn 50 points</Button><Button variant="outline" onClick={() => setFormData({ location: { lat: 12.9716, lng: 77.5946, address: 'GPS adjusted location', ward: 'Ward 12' } })}><LocateFixed className="h-4 w-4" />Use current location</Button></div> : null}</motion.div></AnimatePresence></CardContent></Card>; }
