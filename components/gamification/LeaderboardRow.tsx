import { Avatar, AvatarFallback } from '@/components/ui/avatar'; import type { UserProfile } from '@/lib/types/user';
export function LeaderboardRow({ user, rank }: { user: UserProfile; rank: number }) { return <div className="flex items-center gap-3 rounded-lg border bg-card p-3"><span className="w-8 text-center font-semibold">{rank}</span><Avatar><AvatarFallback>{user.name.slice(0,2).toUpperCase()}</AvatarFallback></Avatar><div className="flex-1"><p className="font-medium">{user.name}</p><p className="text-xs text-muted-foreground">{user.ward}</p></div><p className="font-semibold">{user.points}</p></div>; }
