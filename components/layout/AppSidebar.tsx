'use client';
import Link from 'next/link'; import { usePathname } from 'next/navigation'; import { BarChart3, Map, Medal, PlusCircle, Radar, User, LayoutDashboard } from 'lucide-react'; import { cn } from '@/lib/utils'; import { Avatar, AvatarFallback } from '@/components/ui/avatar';
const nav = [{ href: '/dashboard', label: 'Dashboard', icon: LayoutDashboard }, { href: '/report', label: 'Report', icon: PlusCircle }, { href: '/map', label: 'Map', icon: Map }, { href: '/issues', label: 'Issues', icon: Radar }, { href: '/insights', label: 'Insights', icon: BarChart3 }, { href: '/leaderboard', label: 'Heroes', icon: Medal }, { href: '/profile', label: 'Profile', icon: User }];
export function AppSidebar() { const pathname = usePathname(); return <aside className="hidden w-72 shrink-0 border-r bg-card/70 p-4 lg:flex lg:flex-col"><Link href="/dashboard" className="mb-6 flex items-center gap-3 rounded-md px-2 py-2"><span className="grid h-10 w-10 place-items-center rounded-md bg-primary text-primary-foreground"><Radar className="h-5 w-5" /></span><div><p className="font-semibold">CivicPulse</p><p className="text-xs text-muted-foreground">Your city. Your voice.</p></div></Link><nav className="flex flex-1 flex-col gap-1">{nav.map((item) => { const active = pathname.startsWith(item.href); const Icon = item.icon; return <Link key={item.href} href={item.href} className={cn('flex min-h-11 items-center gap-3 rounded-md px-3 text-sm font-medium text-muted-foreground hover:bg-muted hover:text-foreground', active && 'bg-primary/12 text-primary')}><Icon className="h-4 w-4" />{item.label}</Link>; })}</nav><div className="rounded-lg border bg-background p-3"><div className="flex items-center gap-3"><Avatar><AvatarFallback>AR</AvatarFallback></Avatar><div><p className="text-sm font-medium">Asha Rao</p><p className="text-xs text-muted-foreground">1260 points</p></div></div></div></aside>; }
