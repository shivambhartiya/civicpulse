'use client';
import Link from 'next/link'; import { usePathname } from 'next/navigation'; import { Home, Map, Medal, PlusCircle, Radar } from 'lucide-react'; import { cn } from '@/lib/utils';
const tabs = [{ href: '/dashboard', label: 'Home', icon: Home }, { href: '/map', label: 'Map', icon: Map }, { href: '/report', label: 'Report', icon: PlusCircle }, { href: '/issues', label: 'Issues', icon: Radar }, { href: '/leaderboard', label: 'Heroes', icon: Medal }];
export function MobileNav() { const pathname = usePathname(); return <nav className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-5 border-t bg-background/95 px-2 py-1 backdrop-blur lg:hidden">{tabs.map((tab) => { const Icon = tab.icon; const active = pathname.startsWith(tab.href); return <Link key={tab.href} href={tab.href} className={cn('flex min-h-12 flex-col items-center justify-center gap-1 rounded-md text-[11px] text-muted-foreground', active && 'bg-primary/10 text-primary')}><Icon className="h-4 w-4" />{tab.label}</Link>; })}</nav>; }
