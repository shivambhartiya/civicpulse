'use client';
import { Camera, Upload } from 'lucide-react'; import { Button } from '@/components/ui/button';
export function PhotoUploader({ onFile }: { onFile: (file: File) => void }) { return <label className="flex min-h-44 cursor-pointer flex-col items-center justify-center gap-3 rounded-lg border border-dashed bg-card p-6 text-center hover:bg-muted/40"><Camera className="h-9 w-9 text-primary" /><span className="text-sm font-medium">Capture or upload issue photo</span><span className="text-xs text-muted-foreground">JPEG, PNG, or camera input</span><input className="hidden" type="file" accept="image/*" capture="environment" onChange={(e) => e.target.files?.[0] && onFile(e.target.files[0])} /><Button type="button" variant="outline"><Upload className="h-4 w-4" /> Choose photo</Button></label>; }
