'use client';
import { MapPin } from 'lucide-react'; import { Button } from '@/components/ui/button';
export function LocationPicker({ value, onChange }: { value: { lat: number; lng: number; address: string; ward: string }; onChange: (value: { lat: number; lng: number; address: string; ward: string }) => void }) { return <div className="relative min-h-72 rounded-lg border bg-card map-grid"><div className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-full text-primary"><MapPin className="h-10 w-10 fill-primary/20" /></div><div className="absolute bottom-4 left-4 right-4 rounded-md border bg-background p-3 text-sm"><p className="font-medium">{value.address}</p><p className="text-xs text-muted-foreground">{value.ward} ? {value.lat.toFixed(4)}, {value.lng.toFixed(4)}</p><Button className="mt-3" size="sm" variant="outline" onClick={() => onChange({ ...value, lat: value.lat + 0.001, lng: value.lng + 0.001 })}>Nudge pin</Button></div></div>; }
