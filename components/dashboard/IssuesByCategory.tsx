'use client';
import { Pie, PieChart, ResponsiveContainer, Cell, Tooltip } from 'recharts'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import { CATEGORY_META } from '@/lib/constants/categories'; import { mockIssues } from '@/lib/mock-data';
export function IssuesByCategory() { const data = Object.entries(CATEGORY_META).map(([category, meta]) => ({ name: meta.label, value: mockIssues.filter((issue) => issue.category === category).length, fill: meta.color })).filter((d) => d.value); return <Card><CardHeader><CardTitle>Issues by category</CardTitle></CardHeader><CardContent className="h-72"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={60} outerRadius={95}>{data.map((entry) => <Cell key={entry.name} fill={entry.fill} />)}</Pie><Tooltip /></PieChart></ResponsiveContainer></CardContent></Card>; }
