import { AlertTriangle, CheckCircle2, Clock, Users } from 'lucide-react'; import { Card, CardContent } from '@/components/ui/card'; import { mockIssues } from '@/lib/mock-data';
export function StatsGrid() { const stats = [{ label: 'Open reports', value: mockIssues.filter((i) => !['RESOLVED','CLOSED'].includes(i.status)).length, icon: AlertTriangle }, { label: 'Resolved this week', value: 38, icon: CheckCircle2 }, { label: 'Avg response', value: '19h', icon: Clock }, { label: 'Active heroes', value: '2.4k', icon: Users }]; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map((stat) => { const Icon = stat.icon; return <Card key={stat.label}><CardContent className="flex items-center justify-between p-5"><div><p className="text-sm text-muted-foreground">{stat.label}</p><p className="text-2xl font-semibold">{stat.value}</p></div><span className="grid h-11 w-11 place-items-center rounded-md bg-primary/10 text-primary"><Icon className="h-5 w-5" /></span></CardContent></Card>; })}</div>; }
