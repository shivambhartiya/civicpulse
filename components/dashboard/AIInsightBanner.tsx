import { Sparkles } from 'lucide-react'; import { Card, CardContent } from '@/components/ui/card'; import { mockInsight } from '@/lib/mock-data';
export function AIInsightBanner() { return <Card className="border-primary/40 bg-primary/10"><CardContent className="flex flex-col gap-4 p-5 md:flex-row md:items-start"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-md bg-primary text-primary-foreground"><Sparkles className="h-5 w-5" /></span><div><p className="text-sm font-semibold text-primary">{mockInsight.title}</p><h2 className="text-xl font-semibold">Predictive maintenance signal</h2><p className="mt-1 text-sm text-muted-foreground">{mockInsight.summary}</p></div></CardContent></Card>; }
