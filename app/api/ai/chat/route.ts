import { NextRequest, NextResponse } from 'next/server'; import { mockIssues } from '@/lib/mock-data'; import { ChatSchema } from '@/lib/utils/validation';
export async function POST(req: NextRequest) { const parsed = ChatSchema.safeParse(await req.json()); if (!parsed.success) return NextResponse.json({ error: parsed.error.flatten() }, { status: 400 }); const relatedIssues = mockIssues.filter((issue) => !parsed.data.ward || issue.location.ward === parsed.data.ward).slice(0, 3); return NextResponse.json({ answer: 'CivicPulse found ' + relatedIssues.length + ' relevant reports. The fastest action is to verify nearby issues and watch SLA status.', relatedIssues, suggestedActions: ['Verify one nearby report', 'Enable ward notifications', 'Open predictive hotspot map'] }); }
