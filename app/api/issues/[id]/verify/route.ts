import { NextResponse } from 'next/server'; export async function POST(_: Request, { params }: { params: { id: string } }) { return NextResponse.json({ issueId: params.id, verified: true, pointsAwarded: 10, message: 'Community verification recorded.' }); }
