import { AchievementCard } from '@/components/gamification/AchievementCard'; import { LevelProgress } from '@/components/gamification/LevelProgress'; import { PointsBadge } from '@/components/gamification/PointsBadge'; import { PageWrapper } from '@/components/layout/PageWrapper'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import { BADGES } from '@/lib/constants/badges'; import { mockUsers } from '@/lib/mock-data';
export default function ProfilePage() { const user = mockUsers[0]; return <PageWrapper title="Your civic profile" eyebrow="Participation and rewards" action={<PointsBadge points={user.points} />}><Card><CardHeader><CardTitle>{user.name}</CardTitle></CardHeader><CardContent><LevelProgress level={user.level} points={user.points} /></CardContent></Card><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{BADGES.map((badge, index) => <AchievementCard key={badge.id} name={badge.name} description={badge.description} progress={index === 0 ? 100 : 45} />)}</div></PageWrapper>; }
