import { LeaderboardRow } from '@/components/gamification/LeaderboardRow'; import { PageWrapper } from '@/components/layout/PageWrapper'; import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'; import { mockUsers } from '@/lib/mock-data';
export default function LeaderboardPage() { return <PageWrapper title="Community heroes" eyebrow="Ward vs Ward challenge"><Card><CardHeader><CardTitle>Monthly leaderboard</CardTitle></CardHeader><CardContent className="space-y-3">{mockUsers.map((user, index) => <LeaderboardRow key={user.id} user={user} rank={index + 1} />)}</CardContent></Card></PageWrapper>; }
