steps:
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: bash
    args:
      - '-c'
      - |
        docker build \
          --build-arg NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="${_NEXT_PUBLIC_GOOGLE_MAPS_API_KEY}" \
          --build-arg NEXT_PUBLIC_GOOGLE_MAPS_ID="${_NEXT_PUBLIC_GOOGLE_MAPS_ID}" \
          --build-arg NEXT_PUBLIC_APP_URL="${_NEXT_PUBLIC_APP_URL}" \
          -t ${_REGION}-docker.pkg.dev/$PROJECT_ID/civicpulse/app:${_IMAGE_TAG} \
          .

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/civicpulse/app:${_IMAGE_TAG}'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'civicpulse'
      - '--image=${_REGION}-docker.pkg.dev/$PROJECT_ID/civicpulse/app:${_IMAGE_TAG}'
      - '--region=${_REGION}'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--memory=1Gi'
      - '--cpu=1'
      - '--min-instances=0'
      - '--max-instances=1'
      - '--set-secrets=GEMINI_API_KEY=gemini-api-key:latest'
      - '--set-env-vars=NODE_ENV=production,CIVICPULSE_DATA_DIR=/tmp/civicpulse-data,NEXT_TELEMETRY_DISABLED=1,NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=${_NEXT_PUBLIC_GOOGLE_MAPS_API_KEY},NEXT_PUBLIC_GOOGLE_MAPS_ID=${_NEXT_PUBLIC_GOOGLE_MAPS_ID},NEXT_PUBLIC_APP_URL=${_NEXT_PUBLIC_APP_URL}'

images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/civicpulse/app:${_IMAGE_TAG}'

substitutions:
  _REGION: us-central1
  _IMAGE_TAG: manual
  _NEXT_PUBLIC_GOOGLE_MAPS_API_KEY: ''
  _NEXT_PUBLIC_GOOGLE_MAPS_ID: ''
  _NEXT_PUBLIC_APP_URL: ''
